[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# ──────────────────────────────────────────────────────────────────────────────
# Variables de entorno requeridas (configurar en Netlify UI):
#   Site settings → Environment variables
#
#   DEONE_API_BASE_URL  = https://prueba-supabase.onrender.com/api
#   DEONE_API_KEY       = TK2026A7F9X3M8N2P5Q1R4T6Y8U0I9O3
#
# Con estas variables el frontend usa el proxy server-side (/api/proxy/*)
# y la API key NO queda expuesta en el bundle del cliente.
# ──────────────────────────────────────────────────────────────────────────────
